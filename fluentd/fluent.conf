<source>
  @type forward
</source>

<match docker.**>
 # @type stdout
  type elasticsearch
  logstash_format true
  host 84.240.187.4  # dynamically configured to use Docker's link feature
  port 9200
  flush_interval 5s
</match>
